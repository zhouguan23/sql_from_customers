select max(inserttime) from ipt_invest_land_price

select max(inserttime) from ipt_invest_land_predict

select max(insert_time) from  ipt_invest_actual_price

select max(insert_time) from ipt_invest_aim_price

